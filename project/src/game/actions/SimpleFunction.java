package game.actions;

/**
 * an interface that defines a simple function with no arguments and no return
 */
public interface SimpleFunction {

    /**
     * the main function of the interface, it has no arguments and no return
     */
    void run();
}
